
const baseURL = 'https://reqres.in/'

const APIStrings = {
  loginAPI: baseURL + 'api/login',
  forgotPasswordAPI: baseURL + '',
  userListAPI:baseURL+'api/users?page='
};

export default APIStrings;
